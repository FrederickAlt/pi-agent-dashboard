{
  "name": "pi-agent-dashboard",
  "version": "0.1.0",
  "description": "Session data layer library for Pi Agent Dashboard — scans Pi session directories and produces a unified model",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "bin": {
    "pi-agent-dashboard": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "watch": "tsc --watch",
    "start": "node dist/bin.js"
  },
  "dependencies": {
    "@types/blessed": "^0.1.25",
    "blessed": "^0.1.81",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
